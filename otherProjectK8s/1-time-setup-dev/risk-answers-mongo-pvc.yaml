apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: risk-answers-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: dev-standard-storage-class
  resources:
    requests:
      storage: 1Gi
