apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: risk-answers-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 200Mi
