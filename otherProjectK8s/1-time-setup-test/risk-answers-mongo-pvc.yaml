apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: risk-answers-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: test-standard-storage-class
  resources:
    requests:
      storage: 400Mi

